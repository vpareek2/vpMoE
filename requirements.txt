# Python dependencies for running vpMoE utilities inside the container.
#
# Note: We intentionally do NOT pin or install torch/torchvision here; those are
# provided by the NVIDIA PyTorch base image.

click>=8.2.1
datasets>=4.4.2
flash-linear-attention==0.4.1
huggingface_hub>=0.20.0
immutables>=0.21
#
# Required for GPT-OSS MXFP4 GPU kernels (used by transformers' MXFP4 quantizer).
kernels==0.11.7
openai-harmony==0.0.8
pydantic>=2.10.6
safetensors>=0.5.2
tiktoken>=0.12.0
tokenizers>=0.20.1
tqdm>=4.67.1
accelerate

# NOTE: transformers is installed from the vendored source checkout at
# `src/third_party/transformers` (see `docs/docker.md`).
