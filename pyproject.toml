[project]
name = "vpmoe"
version = "0.0.0"
description = "vpMoE research and training stack (container-first)."
readme = "README.md"
requires-python = ">=3.10"

dependencies = []

[tool.uv]
link-mode = "copy"
